# ⚠️ Deprecated

이 디렉토리(Figma 플러그인 수동 export 경로)는 더 이상 빌드 파이프라인에서 사용하지 않습니다.

토큰 동기화는 **REST API 경로로 통일**되었습니다 (2026-07-09).
`npm run tokens:figma:sync` 사용법은 [`docs/DESIGN_TOKENS_RUNBOOK.md`](../docs/DESIGN_TOKENS_RUNBOOK.md) 참고.

`tokens.json`은 과거 수동 export 백업으로만 남겨두고, 새 커밋에서 갱신하지 않습니다.
